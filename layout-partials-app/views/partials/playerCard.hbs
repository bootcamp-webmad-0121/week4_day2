<div class="col-md-3">
    <article class="player-card">
        <img src="{{photo}}" alt="{{name}}">
        <div>
            <h3>{{name}} {{lastName}}</h3>
            <hr>
            <p>{{team}}</p>
        </div>
    </article>
</div>